
import { render } from '@testing-library/react';
import Tuner from '../components/Tuner';

test('renders Tuner without crashing', () => {
  render(<Tuner />);
  // We can add more specific assertions here
});
