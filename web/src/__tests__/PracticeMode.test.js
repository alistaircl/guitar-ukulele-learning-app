
import { render } from '@testing-library/react';
import PracticeMode from '../components/PracticeMode';

test('renders PracticeMode without crashing', () => {
  render(<PracticeMode />);
  // We can add more specific assertions here
});
